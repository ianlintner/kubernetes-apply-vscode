name: CI (Deprecated)

on:
  workflow_dispatch:

jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
      - name: This workflow is deprecated
        run: |
          echo "This workflow is deprecated and no longer runs automatically."
          echo "Use these workflows instead:"
          echo "- Node CI (.github/workflows/node-ci.yml)"
          echo "- Docs Check (.github/workflows/docs-check.yml)"
